name: openapi-mock

project_files:
  - openapi/.gitkeep
  - docker-compose.openapi-mock.yaml
  - docker-compose.swagger-ui.yaml

ddev_version_constraint: '>= v1.24.3'

removal_actions:
  - rm -rf ${DDEV_APPROOT}/.ddev/openapi